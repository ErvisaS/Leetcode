class Solution {
    public void reverseString(char[] s) {
        int t=s.length-1;
        int i=0;
       while(i<t){
          
            char temp=s[t];
            s[t]=s[i];
            s[i]=temp;
            t--;    
            i++;
            
        }
    }
}
